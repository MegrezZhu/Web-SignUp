# Web-SignUp
##Just an assignment.

created by ZhuChu.

##Instruction
1. Install dependencies.
    
    ```npm
    npm install
    ```
2. This server listens at port `8000`.use `localhost:8000` for accessing.